# Hardware check — DDJ-REV1 against the rebuilt audio pipeline

**Date:** 2026-08-10 (run began the evening of 2026-08-09)
**Ticket:** PHI-171
**Hardware:** Pioneer DDJ-REV1 over USB, MacBook Pro, external display 1920×1280 @ dpr 2
**Checklist:** `CONTRIBUTING.md` → "Hardware check before a show", eight steps
**Instrument:** `test/hardware-probe.js` (dev-only; never referenced by `index.html`)

> **Restored 2026-09-14.** This is the original PHI-171 record, written 2026-08-10 on the
> branch `waskar/phi-171-dj-visualizer-run-the-ddj-rev1-hardware-check-against-the`, which was
> never pushed until 2026-09-14. It replaces a reconstruction that PHI-193 (PR #14) wrote
> because this file could not be found. The figures below are the measured ones.
>
> Line numbers cited below refer to the code as of 2026-08-10. The instrument it describes is the
> original probe (commit `e225ad9`). PR #19 restored that probe as `test/hardware-probe.js`, with
> fixes from the PR #14 review: a hidden-tab gap breaks the FPS run, `sweep()` waits for focus and
> switches back to the starting mode, and `custom` is skipped by default. "Notes on the
> instrument" below still describes the probe as it ran on 2026-08-10.

First time the pipeline rebuilt in #3 has been driven by real hardware. Everything
before this stopped at a simulated microphone.

---

## Verdict

| # | Step | Verdict |
|---|------|---------|
| 1 | Controller connected before browser launch | Pass |
| 2 | Source detected, prefixed `DJ ·`, sorted first | **Pass** |
| 3 | Three bands move independently on a full mix | **Pass** |
| 4 | Bass-solo / hats-solo isolation | **Fail as specified** — see below. Band mapping is correct; the threshold is not applicable to broadband material |
| 5 | BPM settles and holds, including on a heavy mode | **Pass** on load-independence; jitter finding raised separately |
| 6 | All ten modes under real audio, FPS noted | **9 of 10 pass.** Spectrum Bars fails at projection resolution |
| 7 | Fullscreen legibility | **Partial** — external display only, not a projector |
| 8 | Reduce flash damps beat accents | **Pass** |

Additionally verified, not on the checklist: **beat detection genuinely ran in the
`AudioWorklet`**, not the silent render-thread fallback.

---

## Step 2 — device detection

Raw `enumerateDevices()` labels, before the app's cleanup regexes:

```
DDJ-REV1 (2b73:003f)
MacBook Pro Microphone (Built-in)
Serato Virtual Audio (Virtual)
Waskar's iPhone Microphone
Microsoft Teams Audio Device (Virtual)
BlackHole 64ch (Virtual)
Default - MacBook Pro Microphone (Built-in)
```

The controller reports as **`DDJ-REV1 (2b73:003f)`**. It matches `isDJDevice()` on the
existing `ddj` keyword, sorts to position 0, and presents as `DJ · DDJ-REV1 (2b73:003f)`.

**No change to `isDJDevice()` is required.** The DoD's contingency ("record the exact
device label so `isDJDevice()` can be widened") does not apply.

One hazard surfaced instead — see finding F4.

## Step 3 — three bands

Under a full mix at set level: bass ≈ 0.59–0.64, mid ≈ 0.46–0.58, high ≈ 0.16–0.33,
all moving independently. With the stream live and no music, all three read exactly `0`
— the "no `Math.sin` fallback" rule at `CONTRIBUTING.md:151` is holding.

## Step 4 — band isolation on the real signal chain

Measured against the same assertion `test/verify-audio.mjs:123-124` makes: the expected
band must clear `0.05` and exceed the next loudest by `2.5×`.

```
bass solo →  bass 0.274   mid 0.116   high 0.031    ratio 2.36    FAIL (need 2.5)
hats solo →  bass 0.000   mid 0.174   high 0.253    ratio 1.46    FAIL (need 2.5)
```

The threshold was fixed before the run and has not been moved after seeing the numbers.
**As specified, this step fails.**

The evidence nevertheless shows the band mapping is correct, via cross-condition
comparison:

| Band | Bass solo | Hats solo | Change |
|---|---|---|---|
| bass | 0.274 | **0.000** | falls to a hard zero |
| high | 0.031 | 0.253 | rises **8.2×** |

Killing the bass channel zeroes the bass meter; soloing hats raises high eight-fold.
That is not a wiring or channel-mapping fault. What fails is the *test*: `2.5×`
within-condition dominance was calibrated on pure sine tones, where a 100 Hz input has
almost no mid energy. Real music routed through a "bass only" channel carries harmonics
into mid, and hats carry substantial mid. **Step 4 as written cannot pass against a real
mixer regardless of how correct the app is.** See finding F2.

## Step 5 — BPM

Settled **308 ms** after audio start. Held **121 with zero spread** through Particles and
Rings.

Controlled load test, single track, alternating light and heavy modes:

```
light  (particles)  median 122   spread 24
heavy  (polygons)   median 124   spread 22
heavy  (galaxy)     median 125   spread 24
light  (particles)  median 127   spread 26
```

**Render load does not move BPM.** The 2–3 BPM drift continues upward through the return
to a light mode, so it tracks elapsed time rather than load. `audioProcessor.kickNode`
remained a live `AudioWorkletNode` throughout. The claim that detection survives a heavy
render loop is verified against real hardware.

Spread of 22–26 BPM in *every* window, light and heavy alike, is a separate concern —
finding F3.

An earlier uncontrolled pass showed medians shifting across modes (121→132). That is
**not** evidence of load starvation: the track changed mid-session, confounding mode with
tempo. It is recorded here only so the number is not mistaken for a finding later.

## Step 6 — ten modes at projection resolution

Fullscreen, 1920×1280 @ dpr 2 (a 3840×2560 backing store). FPS from sustained focused
frame runs only; `p05` is the number that matters, because a stall is a tail event.

| Mode | n | p50 | p05 | min | |
|---|---|---|---|---|---|
| spectrum | 682 | 25.1 | 21.1 | 16.8 | **FAIL** |
| particles | 533 | 59.9 | 50.3 | 25.0 | pass |
| rings | 540 | 59.9 | 57.1 | 54.9 | pass |
| waves | 541 | 59.9 | 57.1 | 56.2 | pass |
| mandala | 504 | 59.9 | 57.1 | 15.1 | pass |
| tunnel | 499 | 59.9 | 46.7 | 30.3 | pass |
| galaxy | 434 | 59.9 | 45.0 | 26.7 | pass |
| polygons | 358 | 59.5 | 37.0 | 10.6 | pass |
| custom | 301 | 59.9 | 39.2 | 27.6 | pass |
| flow | 370 | 59.9 | 57.1 | 30.4 | pass |

Zero page errors across the sweep. All ten modes reachable via the real `1`–`9`,`0` key
handler.

The same mode measured fullscreen on the **laptop** panel earlier in the session read
p50 59.9 / p05 51.3 over 720 frames. Only the surface changed. See finding F1.

**Mode count:** ten, confirmed at runtime. `CONTRIBUTING.md:126` is correct; PHI-171's
description says nine and is stale. No repo change needed.

## Step 7 — fullscreen legibility

Checked on an external display at 1920×1280, **not a projector**. Operator judgement:
console rail legible from the back of the room; nothing important sits underneath it.

Throw distance and room brightness remain untested. See finding F6.

## Step 8 — Reduce flash

Measured at the consumer — `visualizer.beat`, which every flash and pulse is struck from
(`visualizer.js:253`) — rather than by reading the control back.

```
Reduce flash OFF →  beat p95 0.822,  peak 1.000
Reduce flash ON  →  beat p95 0.124,  peak 0.150   (capped exactly at flashIntensity)
                    84.9% reduction
```

The gating holds at the source, as `CONTRIBUTING.md:159` requires.

---

## Findings — each needs its own ticket

**F1 · Spectrum Bars fails at projection resolution, and the docs recommend it.**
p50 25.1 / p05 21.1 fullscreen at 1920×1280, against 59.9/51.3 on the laptop panel. It is
fill-rate bound. `CONTRIBUTING.md:127` names Spectrum and Particles as the two safe modes
on a slower machine — at projection size Spectrum is the *worst* of the ten and Particles
holds 60. The documented advice is backwards for the one context that matters.

**F2 · Step 4's threshold is unpassable against a real mixer.**
`2.5×` within-condition dominance is a sine-tone criterion applied to broadband material.
Replace with the cross-condition delta actually measured here (bass 0.274 → 0.000; high
0.031 → 0.253), which uses both windows and would still catch a genuine mis-mapping.

**F3 · BPM readout excursions of ~20 BPM on real material.**
Median ~122 with maxima of 143–145 in every window, independent of render load. Not
starvation — the worklet is confirmed active — but the readout is noisier than "settles
and holds" implies.

**F4 · Virtual audio devices are treated as DJ hardware — OBSERVED, not hypothetical.**
`Serato Virtual Audio (Virtual)` matches `isDJDevice()` on the `serato` keyword and sorts
above the built-in microphone. This was noted early in the run as a *plausible* failure
while the real controller was present.

It then happened. Later in the session the DDJ-REV1 dropped off the device list — asleep,
powered down, or unplugged. A page loaded at that moment auto-selected
`Serato Virtual Audio (Virtual)`, presented it as **`DJ · Serato Virtual Audio (Virtual)`**,
and reported **`Ready:`**. No warning, no fallback to the microphone, no indication the
source was a silent loopback.

This is the most dangerous finding of the run, because unlike F1 it fails *quietly*: at
load-in the operator sees `DJ ·` and `Ready`, presses Start, and then debugs a dead
visualizer by looking at the mixer. Two parts to fixing it:

* A device matched only by a virtual/loopback name must not outrank a physical input, and
  a virtual device must never carry the `DJ ·` prefix.
* `Ready:` should not claim readiness for a source producing no signal. A
  "selected but silent" state is exactly the condition the no-`Math.sin`-fallback rule at
  `CONTRIBUTING.md:151` exists to keep visible, and the device rail currently hides it.

**F5 · Deep single-frame stalls in Polygon Collage and Mandala.**
Minima of 10.6 and 15.1 FPS against p05 values of 37.0 and 57.1. Passes the percentile
floor while dropping frames visibly.

**F6 · The projector itself is still unchecked.**
External display is not a projector. Throw distance, room brightness and rail legibility
at real viewing distance need a Pennovation site visit before Founders Alley.

**F8 · The controller dropped off the USB bus three times, unprompted.**
Each time it vanished from CoreAudio and the MIDI device list together, with Serato DJ Pro
still running. It was not unplugged. This is what produced F4's live reproduction, and it
also aborted the MIDI confirmation pass documented in `docs/controllers/ddj-rev1-midi.md`.

Three occurrences in one session is a pattern, not an accident, and the cause is unknown:
cable, connector wear, bus power, a USB hub, macOS USB power management, or the controller
sleeping on its own. It matters more than an ordinary flake because the same disconnection
takes the **audio input** with it — so mid-set it would present as the visualizer dying for
no visible reason, and F4 means the app would then quietly select a virtual device and
report `Ready`.

Worth isolating before the activation: try a different cable, a different port, and a
powered hub, and check whether it correlates with Serato being open or with idle time.

**F7 · Sustained thermal behaviour untested.**
Named in the ticket's background but not one of the eight steps, and twenty minutes
cannot measure it. Needs a full-set soak.

---

## Notes on the instrument

`test/hardware-probe.js` carries four fixes that each produced a *wrong answer* before
being found. They are documented in the file header because they will bite the next run
otherwise:

1. **Focus.** Chrome throttles rAF and timers in a window that is not frontmost. Measured
   that way Spectrum reads 25 FPS on a machine doing 60 — the same number as the genuine
   F1 failure, arrived at for an entirely different reason. Every frame now records
   `document.hasFocus()`, and unfocused frames are excluded rather than averaged in.
2. **Persistence.** The first session lived in a page variable and a stray reload erased
   it. Every mark now snapshots to `localStorage`; `hw.restore()` recovers a run.
3. **Key dispatch.** `app.js:50` switches on `e.code` (`Digit5`), not `e.key`, and the
   guard at `app.js:44` calls `e.target.matches()`, which throws when the event is
   dispatched at `document`. Synthetic keys must go to `document.body` with a `code`.
4. **Self-pacing.** A countdown the operator has to catch is a countdown the operator
   misses while walking to the mixer. Steps needing hands wait for a keypress, with a
   live band readout on screen so isolation can be confirmed before the hold starts.

A driver note: CDP `Runtime.evaluate` times out at 45 s. Keep any single blocking
measurement under ~40 s, or fire it and poll.

---

## PHI-181 findings (append below as the investigation proceeds)

### Session — 2026-08-23

Ran `tools/ddj-rev1-monitor/monitor.py` against the real DDJ-REV1 across
four windows. Log: `tools/ddj-rev1-monitor/ddj-rev1-monitor.log`
(not committed — see `.gitignore`).

**1. Idle baseline** — 15:42–16:30 ET, ~48 min, 548 polls. Controller
connected, Serato not in active use. Zero drops.

**2. Active-use test** — 19:05–22:58 ET, ~3h 53min, thousands of polls.
Serato open, controller in normal use (jog wheels, faders, play/pause,
track loads). Result:

- Two unexplained drops early in this window, close together:
  - **19:14:34 → 19:14:55** (21s). Device left and returned on its own;
    no manual action reported at that time.
  - **19:15:32 → 19:19:24** (~3m 52s). Device left and returned. A manual
    unplug/replug was reported during this session, but the exact time
    was never pinned down — **this gap is inconclusive**, not confirmed
    as either a spontaneous drop or a fully-explained manual action.
- After that, the remaining ~3h 40min of active use ran clean — zero
  further drops. One later drop at 22:58:08–22:58:23 (19s) was a
  deliberately marked manual disconnect/reconnect, used to confirm the
  monitor itself catches a real physical unplug correctly. Confirmed
  working as expected.
- CPU load during the clean stretch stayed low (roughly 5–13 on the
  1-minute average), well under the ~47% Serato CPU noted at the original
  F8 drops.
- The kernel USB log watcher (`log stream` on
  `subsystem == "com.apple.iokit.usb"`) ran the whole session but never
  logged a single matching line, even during confirmed drops — it did
  not contribute useful data this run. Worth checking separately whether
  the predicate or `log stream` needs elevated permissions on this
  machine; the presence-polling channel carried the whole investigation.

**3. Port-swap test** — 22:59–23:31 ET, ~30.5 min, 352 polls. Same
controller, same cable, moved to a different USB port on the Mac
(confirmed via `ioreg`: device enumerated under a new USB node,
`DDJ-REV1@02100000` vs. the original `@01100000`). Zero drops.

**Conclusion: could not reproduce.** Across roughly 4.5 hours of combined
clean runtime (idle, ~3h40min of active use on the original port, and
30.5 min on a swapped port), no confirmed spontaneous drop occurred. One
~4-minute gap (19:15:32–19:19:24) remains unresolved — a manual
unplug/replug was reported around that time but never timestamped
precisely enough to confirm or rule out — and is treated as inconclusive,
not a finding either way.

Given this run's total clean time already exceeds the roughly two hours
the original F8 session covered before its three drops, and a port
change (the cheapest, most likely variable) also ran clean, we are
stopping the investigation here rather than continuing into cable/hub/
power swaps against an unconfirmed problem. No repair/replace/spare-cable
decision is warranted — there is no confirmed defect to act on.

**If this recurs:** re-run `tools/ddj-rev1-monitor/monitor.py` for
another active-use session. Get a precise timestamp for any manual cable
action immediately when it happens — don't reconstruct it afterward, as
that is what left the 19:15 gap unresolved here. Also worth fixing first:
the kernel USB log watcher (`log stream` on `subsystem ==
"com.apple.iokit.usb"`) produced zero output the entire session, even
during confirmed drops — check whether the predicate or `log stream`
needs elevated permissions on this machine before relying on it again.
Untested variables, in order of likely value if a real drop reproduces:
different cable, powered hub, battery vs. mains power.
